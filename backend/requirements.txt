# ── Core agent stack ─────────────────────────────────────────────────────────
# create_agent lives in `langchain`, NOT `langgraph`. Installing langgraph alone
# leaves `from langchain.agents import create_agent` broken (langgraph depends on
# langchain-core, not langchain). Pin langchain and let it pull langgraph.
langchain==1.3.14
langgraph==1.2.9                      # transitively pinned by langchain (>=1.2.5,<1.3.0)
langgraph-checkpoint-postgres==3.1.0  # import path (langgraph.checkpoint.postgres) != pkg name
langchain-groq==1.1.3                 # post-PR#34167: strict=True works on with_structured_output
groq>=0.36.0

# ── Web / API ────────────────────────────────────────────────────────────────
fastapi==0.121.2
uvicorn[standard]==0.40.0
sse-starlette==2.1.3

# ── Database ─────────────────────────────────────────────────────────────────
sqlalchemy[asyncio]==2.0.44
asyncpg==0.30.0
psycopg[binary,pool]==3.2.10          # AsyncPostgresSaver uses psycopg, not asyncpg

# ── Models / config / resilience ─────────────────────────────────────────────
pydantic==2.12.4
pydantic-settings==2.7.1
tenacity==9.1.2
python-dotenv==1.1.1

# ── Tests ────────────────────────────────────────────────────────────────────
pytest==8.4.2
pytest-asyncio==1.2.0
httpx==0.28.1
